import { Container } from '../../styles'
import { FormDiv, Formulario, HeroTitle } from './styles'

const Hero = () => (
  <Formulario>
    <Container>
      <FormDiv>
        <HeroTitle>
          As melhores vagas para tecnologia, design e artes visuais.
        </HeroTitle>
      </FormDiv>
    </Container>
  </Formulario>
)

export default Hero
